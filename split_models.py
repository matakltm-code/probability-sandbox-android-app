import os

with open('app/src/main/java/com/example/ActivityLogDB.kt', 'r') as f:
    activity_log_content = f.read()

# Extract ActivityLog and ActivityLogDao
# Extract AppDatabase
# Extract ActivityLogRepository
# Extract ActivityLogViewModel

# I can just split the file manually by using regular expressions or just rewriting it.
